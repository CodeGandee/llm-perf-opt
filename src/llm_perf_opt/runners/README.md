Runners (`llm_perf_opt.runners`)

Executable adapters that implement a minimal interface for driving model
execution (e.g., prefill and decode). Implementations may target PyTorch, vLLM,
or TensorRT-LLM.

