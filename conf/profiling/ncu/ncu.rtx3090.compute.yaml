# Nsight Compute preset: RTX 3090 (GA102) — Compute focus

target_processes: all
nvtx_include: decode*
set: roofline
# Focused sections for compute-bound analysis
sections:
  - SpeedOfLight
  - ComputeWorkloadAnalysis
  - Occupancy
  - SchedulerStats
  - WarpStateStats
  - LaunchStats
  - InstructionStats
# Avoid explicit metrics; let section defaults apply to keep compatibility
metrics: null
csv: true

